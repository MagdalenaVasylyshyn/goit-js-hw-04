const profile = {
  username: 'Jacob',
  playTime: 300,
  changeUsername(newName) {
    for (const usernam of this.username) {
      if (this.usernam === this.username) {
        this.usernam = newName;
      }
    }
  },
  updatePlayTime(hours) {
    for (const usernam of this.username) {
      if (this.usernam === this.username) {
        this.usernam = newName;
      }
    }
  },
  getInfo() {},
};
